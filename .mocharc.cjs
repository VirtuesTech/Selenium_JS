module.exports = {
    timeout: 12000,           // Set a static timeout of 5000ms
    reporter: 'mochawesome', // Use Mochawesome for reporting
    recursive: true          // Include subdirectories for test discovery
  };
  